@model IEnumerable<ApiStudyBuddy.Models.FlashCard>

@{
    ViewData["Title"] = "Flashcards";
    Layout = "~/Views/MyStudies/_myStudiesLayout.cshtml";
}

<script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/js/bootstrap.bundle.min.js"></script>

<h1>Flashcards</h1>

<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.FlashCardId)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.FlashCardQuestion)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.FlashCardAnswer)
            </th>
            <th>
            </th>
        </tr>
        @foreach (var item in Model) 
        {
            <tr>
                <td>
                    @Html.DisplayFor(modelItem => item.FlashCardId)
                </td>
                <td>
                    @Html.DisplayFor(modelItem => item.FlashCardQuestion)
                </td>
                <td>
                    @Html.DisplayFor(modelItem => item.FlashCardAnswer)
                </td>
                <td>
                    <a asp-controller="MyStudies" asp-action="AddToDeck" class="btn btn-primary" asp-route-id="@item.FlashCardId">Add To a Deck</a>
                   @*  <button type="button" class="btn btn-primary" onclick="@item.FlashCardId" data-bs-toggle="modal" data-bs-target="@("#addtoDeck-"+ @item.FlashCardId)">
                        Add to a Deck
                    </button>

                    <div class="modal fade" id="addtoDeck-@item.FlashCardId" tabindex="-1" aria-labelledby="staticBackdropLabel" aria-hidden="true">
                        <div class="modal-dialog">
                            <div class="modal-content">
                                <form asp-action="AddToDeck" asp-controller="MyStudies" id="AddtoDeck" method="post">
                                    <div class="modal-header bg-info">
                                        <h1 class="modal-title fs-5" id="staticBackdropLabel">Add Flashcard to a Deck.</h1>
                                        <button type="button" class="btn-close" data-bs-dismiss="modal" aria-label="Close"></button>
                                    </div>
                                    <div class="modal-body">
                                        <div>
                                            <h2>Select a deck</h2>
                                        </div>
                                        
                                    </div>
                                    <div class="modal-footer">
                                        <button type="button" class="btn btn-secondary" data-bs-dismiss="modal">Cancel</button>
                                        <button type="submit" id="create-flashcard-btn" class="btn btn-primary" data-toggle="modal">Add</button>
                                    </div>
                                </form>
                            </div>
                        </div>
                    </div>  *@
                </td>
                   
            </tr>
        }
    </thead>
</table>

@* @section scripts {
    <script>
        $("#add-to-Studies-btn").click(function () {
            $("#addtoDeck-modal").modal('show');
        });
    </script>*@
